@*@using SportStore.Domain.Entities
    @model IEnumerable<SportStore.Domain.Entities.Product>*@
@model SportStore.WebUI.Models.ProductsListViewModel

@{
    ViewBag.Title = "Products";
}

@foreach (var p in Model.Products)
{
    @*<div class="well">
        <h3>
            <strong>@p.Name</strong>
            <span class="pull-right label label-primary">@p.Price.ToString("c")</span>
        </h3>   
        <span class="lead">@p.Description</span>
    </div>*@
    @Html.Partial("ProductSummary", p)
}

<div class="btn-group pull-left">
    @Html.PageLinks(Model.PagingInfo, x => Url.Action("List", new { page = x, category = Model.CurrentCategory }))
</div>

<br/>
<br />
<br />

@*http://getbootstrap.com/components/*@


<div type="button" class="btn btn-default btn-lg">
    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>@Html.ActionLink("아이템 추가", "Add")
</div>

<div type="button" class="btn btn-default btn-lg">
    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>@Html.ActionLink("아이템 삭제", "Remove")
</div>